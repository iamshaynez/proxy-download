我希望构建一个具有前端页面和后端服务的网站，用来代理下载任何文件，基本功能如下：

- 网站有一个简单的密码保护，用户需要输入密码（不需要用户名）后才能访问。密码通过后端环境变量注入管理。
- 网站前端提供用户输入：目标下载文件的下载 URL 路径。
- 用户点击远端下载按钮，触发后端下载。
- 后端应该根据这个 URL，开始下载，下载完成后，除非目标文件本身就是 zip 文件，否则在本地压缩成 zip。

- 前端页面将展示所有已下载的文件（应该是压缩成 zip 的），用户可以下载，或者删除。


最终项目应该可以以 docker 部署，所以需要对应的配置文件。